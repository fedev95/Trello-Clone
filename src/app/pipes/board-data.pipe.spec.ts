import { BoardDataPipe } from './board-data.pipe';

describe('BoardDataPipe', () => {
  it('create an instance', () => {
    const pipe = new BoardDataPipe();
    expect(pipe).toBeTruthy();
  });
});
